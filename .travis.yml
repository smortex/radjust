language: c

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - cmake
      - ruby-bundler
      - libssl-dev

script: cmake . && make && bundle install --jobs=3 --retry=3 && bundle exec cucumber
